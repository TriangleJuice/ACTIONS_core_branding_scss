name: Deploy to AWS TEST
on:
  push:
    tags:
      - v*
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Use Node 10.x
        uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - name: Install dependencies
        run: npm ci
      - name: Build library
        run: npm run aws
      - name: Archive build
        if: success()
        uses: actions/upload-artifact@v1
        with:
          name: dist
          path: dist
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Download build
        uses: actions/download-artifact@v1
        with:
          name: dist
      - name: Get tag
        uses: battila7/get-version-action@v2
      - name: Upload to Amazon S3
        uses: shallwefootball/s3-upload-action@master
        with:
          aws_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws_secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws_bucket: ${{ secrets.AWS_BUCKET }}
          source_dir: dist/${{ steps.get_version.outputs.version-without-v }}
          destination_dir: /${{ github.event.repository.name }}/${{ steps.get_version.outputs.version-without-v }}
      # - name: Deploying application to Amazon S3
      #   uses: dvelasquez/deploy-s3-action@main
      #   with:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     AWS_DEFAULT_REGION: ${{ secrets.AWS_DEFAULT_REGION }}
      #     AWS_BUCKET_NAME: ${{ secrets.AWS_BUCKET }}
      #     BUCKET_PATH: /${{ github.event.repository.name }}/${{ steps.get_version.outputs.version-without-v }}
      #     DIST_LOCATION_CODE: ./dist/${{ steps.get_version.outputs.version-without-v }}
